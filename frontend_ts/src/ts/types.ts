export const enum TrackType {
  car = 1,
  bicycle = 2,
  walk = 3,
  other = 4,
}

export const enum TrackStatus {
  done = 1,
  planned = 2,
}

export const enum AlertStatus {
  success = 1,
  danger = 2,
}
