export const enum TrackType {
  car = 1,
  bicycle = 2,
  walk = 3,
  other = 4,
  mushroom = 5,
}

export const enum TrackStatus {
  done = 1,
  planned = 2,
  trail = 3,
  other_people = 4,
  event = 5,
  border = 6,
}

export const enum AlertStatus {
  success = 1,
  danger = 2,
}
